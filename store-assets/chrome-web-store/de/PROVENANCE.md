# Herkunft der Store-Screenshots

Die fünf Bilder wurden am 18. September 2026 direkt aus der lokal geladenen Erweiterung `Benni New Tab` aufgenommen.

- Quelle: Git-Commit `8b09905`
- Browser: Microsoft Edge `153.0.4234.32`
- Sprache: Deutsch (`de-DE`)
- Aufnahmeformat: exakt `1280 × 800` Pixel
- Exportformat: 24-Bit-PNG (`RGB`, PNG-Farbtyp 2, ohne Alphakanal)
- Feste Uhrzeit für reproduzierbare Aufnahmen: `09:41`

Die Bilder zeigen echte App-Zustände. Es wurden keine Bedienelemente, Texte oder Funktionen nachträglich ergänzt. Für eine realistische Darstellung wurden ausschließlich die vorhandenen Funktionen zum Anpinnen und Anlegen eigener Shortcuts verwendet. Die Screenshots wurden anschließend nur technisch von RGBA nach RGB konvertiert, damit sie die Vorgabe „kein Alpha“ erfüllen.

Die fünf PNG-Dateien im Unterordner `screenshots` sind die hochzuladenden Store-Assets. `manifest.json` und diese Datei dienen nur der internen Dokumentation und werden nicht in den Chrome Web Store hochgeladen.
